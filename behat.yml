# behat.yml
default:
    context:
        parameters:
            subcontext:
                WebContext: ~
    extensions:
        mink_extension.phar:
            mink_loader: mink.phar
            goutte: ~
            selenium2: ~